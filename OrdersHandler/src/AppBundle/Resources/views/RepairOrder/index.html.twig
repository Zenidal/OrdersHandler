{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RepairOrder list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Description</th>
                <th>Address</th>
                <th>Status</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('repairorder_show', { 'id': entity.id }) }}">{{ entity.description }}</a></td>
                <td>{{ entity.address }}</td>
                <td>{{ entity.status }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('repairorder_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('repairorder_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('repairorder_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
